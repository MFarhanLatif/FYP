@model CustDAccess.CustRegistration

@{
    ViewBag.Title = "Login";
}

@using (Html.BeginForm("Login", "Home", FormMethod.Post))
{
    <fieldset>
        <legend>Mvc Simple Login Application Demo</legend>

        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true)
        @if (@ViewBag.Message != null)
        {
            <div style="border: 1px solid red">
                @ViewBag.Message
            </div>
        }
        <table>
            <tr>
                <td>@Html.LabelFor(a => a.CustEmail)</td>
                <td>@Html.TextBoxFor(a => a.CustEmail)</td>
                <td>@Html.ValidationMessageFor(a => a.CustEmail)</td>
            </tr>
            <tr>
                <td>
                    @Html.LabelFor(a => a.CustPassword)
                </td>
                <td>
                    @Html.PasswordFor(a => a.CustPassword)
                </td>
                <td>
                    @Html.ValidationMessageFor(a => a.CustPassword)
                </td>
            </tr>
            <tr>
                <td></td>
                <td>
                    <input type="submit" value="Login" />
                </td>
                <td></td>
            </tr>
        </table>
    </fieldset>
}

<h2>Login</h2>

