@model CustDAccess.CustRegistration

@{
    ViewBag.Title = "UserDashBoard";
}
< fieldset >
    < legend > User DashBoard < /legend>

@if (Session["CustEmail"] != null)
{
   <text >
    Welcome @Session["CustEmail"].ToString()
 </text>
    } < /fieldset>  
<h2>UserDashBoard</h2>

